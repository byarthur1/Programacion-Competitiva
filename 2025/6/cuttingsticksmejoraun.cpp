#include <bits/stdc++.h>
using namespace std;

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(0), cout.tie(0);

    int l, n;
    cin >> l;
    while(l){
        cin >> n;


        cin >> l;
    }

    return 0;
}